# Orchestrator Notes
- Branch `feat/orchestrated-transition`; last conventional commit `build(test): make local build/test reproducible (sass/certs/playwright)`.
- Working tree: `dist/index.html` updated with Planner Database panel; `src/package.json`/`src/playwright.config.ts` tweaked for local tooling; untracked `dist/js/planner-db.js`, `dist/js/sqlite3.*`, `src/scripts/run-playwright-with-summary.mjs`, `src/tests/planner-basics.spec.ts`.
- Baseline prep completed (`npm ci`, `npm run build`, `npm run setup:certs`, `npx playwright install --with-deps`).
- `npm test` failed (6 specs): selectors in `default-homepage` & `url-sharing` now see two headings; `planner-basics` hits SQLite WASM export (`this.db.export` undefined). Report: `src/test-results/results.json`, HTML at `src/playwright-report`.
- PRD alignment: M0 is partially implemented (in-browser SQLite harness) but missing File System Access and integration. Milestones M1-M6 remain untouched.
- Next actionable scope: adjust Playwright selectors for the new planner heading and update SQLite export implementation so `planner-basics` passes, then re-run targeted tests before full suite.
- Updated Playwright selectors in default-homepage/url-sharing specs for the Planner DB heading and normalized the "Change Colors" assertions.
- Replaced the wasm export shim: Planner DB now uses sqlite3_deserialize with managed buffers; planner-basics spec waits for database readiness before assertions.
- Targeted tests: `npm test -- tests/default-homepage.spec.ts tests/url-sharing.spec.ts tests/planner-basics.spec.ts` (pass); full suite `npm test` (92 tests) now green. Latest summary in `src/test-results/results.json`, HTML report under `src/playwright-report`.
- Added File System Access-aware planner DB manager (`dist/js/planner-db.js`) with sqlite3_deserialize import/export, auto-save to handles, and safe fallbacks.
- Updated planner UI copy for create/open buttons to reflect file-based flows and Playwright specs for heading selectors and capability checks.
- Ran `npm test -- tests/default-homepage.spec.ts tests/url-sharing.spec.ts tests/planner-basics.spec.ts` and full `npm test`; both green (reports under `src/test-results`).
- Added Save button + auto-save fallback; File System Access flows persist directly while non-supported browsers download copies. Update planner-basics Playwright spec accordingly.
- Wired Planner DB Save button to persist File System Access handles and auto-save after DAO writes/migrations (`dist/js/planner-db.js`).
- Playwright planner bootstrap spec now confirms Save button disabled without handles (`src/tests/planner-basics.spec.ts`); targeted `npm test -- tests/planner-basics.spec.ts` and full `npm test` both green.
- Audited dist/js/main.js: calculator state lives in global subnetMap (nested CIDR tree with _note/_color per leaf), operatingMode, and maxNetSize; UI binds split/join/note/color events to mutate the tree, while import/export + URL sharing serialize the same structure via LZ-String. Mapping this tree to SQLite will need hierarchical subnets, note/color columns, and persistence of operating mode/base network.
- Drafted M1 wiring plan: extend SQLite schema with \planner_state\ (base_network, operating_mode) and \planner_subnet\ (id, parent_id, cidr, note, color, ordinal) plus cascading deletes; expose DAO helpers on PlannerDbManager to load/save tree, and refactor main.js reset/split/join paths to call async DAO methods (transactions wrapping mutate_subnet_map) while renderTable consumes DB query results.
- Playwright coverage plan: new planner-persistence.spec.ts to split/join with File System Access mock and validate page reload pulls from SQLite; extend planner-basics.spec.ts to assert DAO resets from DB; adapt deep-functional.spec.ts to operate via DB fixtures once URL sharing removed; keep fallback coverage by simulating in-memory mode when FSA unavailable.
- Added planner_state/planner_subnet migrations (user_version 2) and PlannerDbManager snapshot helpers; new Playwright spec (src/tests/planner-snapshot.spec.ts) exercises save/load tree flows.
- Hooked planner UI actions (reset/split/join/note/mode) to SQLite snapshot persistence with debounce helper; exposed subnetMap globally for Playwright asserts; added change event to bind text inputs.
- Targeted `npm test -- tests/planner-snapshot.spec.ts` and full `npm test` now green; reports at `src/test-results/results.json` and Playwright HTML bundle.
- Refactored `renderTable` to hydrate from SQLite snapshots and keep form inputs synchronized (`dist/js/main.js:260`, `dist/js/main.js:305`).
- Snapshot helpers now centralize persistence scheduling and post-save refresh (`dist/js/main.js:1179`, `dist/js/main.js:1305`, `dist/js/main.js:1445`).
- Tests: `npm test -- tests/planner-basics.spec.ts tests/planner-snapshot.spec.ts`; `npm test` (96 specs).

- **M1.5 Schema Reconciliation & Cleanup** (commit aaaa171): Removed 8 unused v1 migration tables (tbl_building, tbl_floors, tbl_idf, tbl_rack, tbl_device, tbl_supernet, tbl_subnet, tbl_interface) as they were never used by the application. Migration v1 now intentionally empty placeholder for future PRD v5. Deprecated insertSampleBuilding() and listBuildings() methods (no-ops). Updated planner-basics.spec.ts to expect schema v3 (was v2), removed assertions for removed tables, fixed test to not artificially reset user_version. Completely rewrote app/database-schema.md to document actual v3 schema (planner_state, planner_subnet, planner_vrf only) with migration history and cleanup rationale. Schema version remains at 3 (TARGET_VERSION = 3). Tests: planner-basics (2/2) and planner-snapshot (4/4) passing.

- **M2 Enhanced Subnet Metadata & Validation** (commit 630a6b2): Discovered M2 UI and validation features were already fully implemented in dist/js/main.js (lines 307-453 for UI rendering, lines 1665-1820 for validation). All required features present: VLAN input (0-4094 range with uniqueness check), gateway IP input (IPv4 validation with subnet range checking), VRF dropdown (populated from planner_vrf table), purpose dropdown (LAN/MGMT/INTERCONNECT/OTHER with /30 or /31 size requirement for INTERCONNECT). Validation logic enforces constraints via modal warnings and reverts invalid inputs. Persistence and hydration functions (mapToSnapshotNodes, snapshotTreeToMap) correctly save/load all metadata fields to/from SQLite. Work focused on adding comprehensive E2E tests to planner-snapshot.spec.ts: (1) VLAN collision test validates uniqueness enforcement across subnets, (2) gateway range test validates IP must fall within usable subnet range (respecting operating mode reserved IPs), (3) VLAN/gateway/VRF/purpose persistence test validates full round-trip through SQLite save/load cycle, (4) INTERCONNECT purpose validation test validates size constraints (/30 or /31 only). All 4 new tests use data attributes for element targeting, explicit change event dispatching for reliability, and appropriate timeouts for async operations. Tests: planner-snapshot (6/6, 12 total tests across chromium and firefox) passing. No code changes required - M2 features already production-ready.
